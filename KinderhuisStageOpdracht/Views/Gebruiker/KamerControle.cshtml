@model KinderhuisStageOpdracht.Models.Viewmodels.GebruikerViewModel.ListKamerControleItemsViewmodel
@{
    ViewBag.Title = "KamerControle";
}
@using (Html.BeginForm("KamerControle", "Gebruiker", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    <h2>KamerControle</h2>
    <table id="kamercontroleTable">
        <tr>
            <th>Taak</th>
            <th>Kleine beschrijving</th>
            <th>Heb ik dit gedaan</th>
        </tr>

        @*@foreach (var o in Model.KamerControleItems)
        {
            <tr>
                <td>
                    <div id="kamercontroleTitel">@Html.DisplayFor(modelItem => o.Titel)</div>
                    <img src="@Url.Content(o.ImageUrl)" class="kamerControleImage"/>
                </td>

                <td>@Html.DisplayFor(modelItem => o.Beschrijving)</td>

                <td>@Html.EditorFor(modelItem => o.DoneClient)</td>
            </tr>
        }*@

        @for (int i = 0; i < Model.KamerControleItems.Count(); i++)
        {
            <tr>
                <td>
                    <div id="kamercontroleTitel">@Html.DisplayFor(modelItem => Model.KamerControleItems[i].Titel)</div>
                    @Html.HiddenFor(modelItem => Model.KamerControleItems[i].Titel)
                    <img src="@Url.Content(@Model.KamerControleItems[i].ImageUrl)" class="kamerControleImage" />
            </td>
                <td>@Html.DisplayFor(modelItem => Model.KamerControleItems[i].Beschrijving)</td>

                <td>@Html.EditorFor(modelItem => Model.KamerControleItems[i].DoneClient)</td>
        </tr>
        }
    </table>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" class="btn btn-default" value="Opslaan" />
        </div>
    </div>
}
